# Erlang-Static-Single-Assignment
An erlang project with some functions that can be used to process expressions in static single assignment form, as well as translating standard expressions to SSA form.
